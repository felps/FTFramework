/*package tests;

import java.util.Vector;

import org.integrade.portlets.LocalRepository;
import org.integrade.portlets.RepositoryFile;

import clusterManagement.DirectoryNotFoundException;
import clusterManagement.InvalidPathNameException;
import clusterManagement.SecurityException;

import asct.core.ApplicationControlFacade;

public class Main {

	/**
	 * @param args
	 */
/*	public static void main(String[] args) {
		LocalRepository local = new LocalRepository("lundberg");
		try {
			RepositoryFile root = new RepositoryFile(new ApplicationControlFacade(local.getTempPath()));
			Vector v = root.fileList();
			Vector vv = root.fileTree();
			for (int i = 0; i < v.size(); i++)
				System.out.println(i + (String)((RepositoryFile)v.get(i)).getPath());
			System.out.println("");
			for (int i = 0; i < vv.size(); i++)
				System.out.println(i + (String)((RepositoryFile)vv.get(i)).getPath());
		} catch (SecurityException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}

	}

}*/
